import styled from "styled-components";
// Lib polished permite trabalhar com cores dentro do styled components.
import { darken } from "polished";

export const Wrapper = styled.div``;

export const Content = styled.div``;
